import React from 'react';

const Hero = ({title, subTitle}) => (
  <div className="hero-container">
    <h2>Full stack javascript developer</h2>
    <p>I love building React and Angular apps</p>
  </div>
);

export default Hero;
